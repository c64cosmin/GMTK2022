[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;
render_mode blend_mix;

uniform sampler2D material;

void fragment(){
	COLOR = texture(TEXTURE, UV) * texture(material, UV);
}"
